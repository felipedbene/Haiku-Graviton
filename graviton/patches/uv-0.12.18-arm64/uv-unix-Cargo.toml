[package]
name = "uv-unix"
version = "0.0.85"
description = "Unix-specific functionality for uv"
edition = { workspace = true }
rust-version = { workspace = true }
homepage = { workspace = true }
repository = { workspace = true }
authors = { workspace = true }
license = { workspace = true }

[lib]
doctest = false
test = false

[lints]
workspace = true

# Haiku has no `nix::sys::resource`. Use libc directly through a small shim.
[target.'cfg(all(unix, not(target_os = "haiku")))'.dependencies]
nix = { workspace = true }

[target.'cfg(target_os = "haiku")'.dependencies]
libc = { workspace = true }

[target.'cfg(unix)'.dependencies]
thiserror = { workspace = true }
